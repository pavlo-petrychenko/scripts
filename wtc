#!/usr/bin/env bash
# wtc - open Zed in current worktree directory
# Usage: wtc
#
# Runs under tmux and cmux (see wt-mux.sh).

set -euo pipefail

. "${WT_LIB:-$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/wt-mux.sh}"

zed "$(mux_current_path)"
